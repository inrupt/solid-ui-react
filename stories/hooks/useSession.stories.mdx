import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Hooks/useSession" />

# useSession

A hook to access the current session from context, along with related values.

```
const {
    session,
    sessionRequestInProgress,
    fetch,
    login,
    logout,
  } = useSession()
```

## Return Values

- `session`: the Session retrieved from context
- `sessionRequestInProgress`: a boolean indicating whether a session request (e.g. login) is currently in sessionRequestInProgress
- `fetch()`: the fetch method of the current session
- `login(options?)`: function to login. Takes optional object of options, returns a promise
- `logout()`: function to logout. Returns a promise
